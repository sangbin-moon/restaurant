<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.restaurant.service.impl.AdminUserMapper">

    <select id="login"
            parameterType="egovframework.restaurant.vo.AdminUserVO"
            resultType="egovframework.restaurant.vo.AdminUserVO">

        SELECT
            ADMIN_ID AS adminId,
            LOGIN_ID AS loginId,
            LOGIN_PASSWORD AS loginPassword,
            ADMIN_NAME AS adminName,
            USE_YN AS useYn,
            CREATED_AT AS createdAt
        FROM ADMIN_USER
        WHERE LOGIN_ID = #{loginId}
          AND LOGIN_PASSWORD = #{loginPassword}
          AND USE_YN = 'Y'

    </select>

</mapper>